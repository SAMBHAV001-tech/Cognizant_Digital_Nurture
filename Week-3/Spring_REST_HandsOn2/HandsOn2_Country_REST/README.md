Hands-on 2 – REST Country Web Service

What is JSON Serialization
When a client requests a resource from a REST controller, Spring Boot auto-detects Jackson in the classpath and serializes Java beans directly to JavaScript Object Notation (JSON) format before returning the response payload.
