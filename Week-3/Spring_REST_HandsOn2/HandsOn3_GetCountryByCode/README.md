Hands-on 3 – Get Country based on Country Code

What is @PathVariable
The @PathVariable annotation binds a URI template placeholder variable (e.g. {code} in /countries/{code}) directly to a controller method parameter. This allows REST APIs to accept dynamic values natively as part of the path structure.
