Hands-on 1 – Create Static Employee Data

What are DAO objects in Spring
A Data Access Object (DAO) handles database or XML-based data retrieval operations. Load config definitions from external XML and encapsulate static collection lists mapping domain relationship beans (e.g. Skill, Department, Employee).
