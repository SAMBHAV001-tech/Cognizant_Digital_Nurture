# Hands-on 3 – Bean Validation

This hands-on demonstrates adding validation constraints on inputs to enforce formatting, size constraints, and null checks on fields of request payloads.

## Constraints used
- `@NotNull`: Specifies that the country code field cannot be null.
- `@Size`: Restricts the country code size to exactly 2 characters.
