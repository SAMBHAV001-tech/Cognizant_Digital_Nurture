# Hands-on 1 – REST POST for Country

This hands-on demonstrates creating a POST endpoint to add a new country to the list and structure endpoints according to REST URL guidelines.

## REST URL Guidelines
- Use plural nouns for resources (e.g. `/countries` instead of `/getCountry`).
- Use HTTP POST to create new resource instances.
