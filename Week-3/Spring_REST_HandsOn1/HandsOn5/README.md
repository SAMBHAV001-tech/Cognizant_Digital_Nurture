Hands-on 5 – Singleton and Prototype Scope

What are Spring Bean Scopes
- Singleton (Default): The Spring container creates exactly one instance of the bean per ApplicationContext. Subsequent requests for the same bean ID return the same shared instance.
- Prototype: The Spring container creates a new instance of the bean every single time it is requested.
