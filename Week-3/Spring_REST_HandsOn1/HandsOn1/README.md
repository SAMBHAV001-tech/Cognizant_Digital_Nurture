Hands-on 1 – Create Spring Boot Web Project

What is Spring Boot
Spring Boot is an extension of the Spring framework that eliminates much of the boilerplate configuration required for setting up a Spring application.

Dependencies Used
- Spring Boot DevTools: Provides developer features like automatic restarts when files change.
- Spring Web: Includes Spring MVC, REST controllers, and Tomcat as the embedded server.
