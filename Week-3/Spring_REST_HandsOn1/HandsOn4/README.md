Hands-on 4 – Load Country from Spring XML

What is Dependency Injection
Dependency Injection is a design pattern where an object's dependencies are injected from the outside (by the framework container) rather than instantiated inside the object itself. In XML configuration, properties are injected using <property name="..." value="..."/> elements, which map to setter methods in the Java class.
