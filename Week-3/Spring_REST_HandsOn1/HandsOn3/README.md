Hands-on 3 – Incorporate Logging

What is Spring Boot Logging Configuration
Spring Boot uses SLF4J and Logback for logging by default. Logging properties inside application.properties allow configuring logging levels per package (e.g. logging.level.com.cognizant=DEBUG) and defining custom console output patterns.
